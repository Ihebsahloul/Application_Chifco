<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/PrimaryColor</item>
        <item name="android:colorPrimaryDark">@color/PrimaryDarkColor</item>
        <item name="windowActionBar">false</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:textColorPrimary">@color/TextColor</item>
        <item name="colorPrimaryDark">@color/TextColor</item>
        <item name="android:colorForeground">@color/TextColor</item>
        <item name="android:textColorSecondary">@color/TextColor</item>
        <item name="colorAccent">@color/link_text_material_light</item>
        <item name="android:textColorPrimaryInverse">@color/abc_primary_text_disable_only_material_dark</item>
        <item name="colorPrimary">@color/purewhite</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="fgfdgfdgfdgfd" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="new_theme" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:textColorPrimary">@color/TextColor</item>
    </style>

</resources>
